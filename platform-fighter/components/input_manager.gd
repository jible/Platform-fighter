extends Node

"""
LOOKING FOR A MODULAR WAY TO MANAGE INPUTS FROM MULTIPLE PLAYERS AT ONCE

options:
	-emit input signals with player as argument.
	Standardize inputs to the current game's language. 
	Rather than attack and dash, say side special and up special
	Easy way for the game to automatically map inputs to a character based on player number
Global input managers are significatly easier to use for online multiplayer, so it will be global
"""
